export function confirmCheckDetails(value) {
  return `input[name="confirmCheckDetails"][value="${value}"]`;
}

export const terms = "#terms";

export function typeOfLivestock(liveStock) {
  return `input[name="typeOfLivestock"][value="${liveStock}"]`;
}

export function typeOfReview(reviewType) {
  return `input[name="typeOfReview"][value="${reviewType}"]`;
}

export const visitDateDay = "#visit-date-day";

export const visitDateMonth = "#visit-date-month";

export const visitDateYear = "#visit-date-year";

export function whenTestingWasCarriedOut(value) {
  return `input[name="whenTestingWasCarriedOut"][value="${value}"]`;
}

export function speciesNumbers(value) {
  return `input[name="speciesNumbers"][value="${value}"]`;
}

export const numberOfAnimalsTested = "#numberAnimalsTested";

export const vetsName = "#vetsName";

export const vetRcvsNumber = "#vetRCVSNumber";

export function sheepEndemicsPackage(endemicsPackage) {
  return `input[name="sheepEndemicsPackage"][value="${endemicsPackage}"]`;
}

export function sheepTestsDiseaseCondition(diseaseCondition) {
  return `input[name="sheepTests"][value="${diseaseCondition}"]`;
}

export function testResult(value) {
  return `input[name="testResult"][value="${value}"]`;
}

export function piHuntForBVDDone(value) {
  return `input[name="piHunt"][value="${value}"]`;
}

export function piHuntDoneForAllCattle(value) {
  return `input[name="piHuntAllAnimals"][value="${value}"]`;
}

export const laboratoryURN = "#laboratoryURN";

export function followUpTestResults(value) {
  return `input[name="testResults"][value="${value}"]`;
}

export function biosecurity(value) {
  return `input[name="biosecurity"][value="${value}"]`;
}

export const submitClaim = "#submit-claim";

export const reference = "#reference";
